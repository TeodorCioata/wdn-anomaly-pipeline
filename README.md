# wdn-anomaly-pipeline
A modular, configuration-driven Python pipeline that wraps EPANET/WNTR to systematically generate large-scale, labelled time-series datasets for water distribution networks (WDNs).
